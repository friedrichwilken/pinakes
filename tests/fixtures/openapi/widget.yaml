openapi: 3.0.3
info:
  title: Example
  version: "1"
components:
  schemas:
    Widget:
      type: object
      description: A small widget.
      required: [name]
      properties:
        name:
          type: string
          description: Display name.
        tags:
          type: array
          items:
            type: string

# Notification Templates

The platform sends email notifications for account events such as password resets,
billing changes and security alerts. Templates control the wording and layout of
every message the platform sends.

## Customizing templates

Copy the default template files into your project and edit the subject line, the body
text and the footer. Restart the service after changing a template so the new version
is loaded into memory. Keep a backup first.

# Getting started

The service stores project files and serves them to clients over HTTPS.
This page walks through the pieces you need before the first deployment.

## What you need

- a Linux host with 2 GB of memory
- a domain name pointing at the host
- a storage backend: local disk or an object store

## Next steps

Install the service, create a project and configure a storage backend.

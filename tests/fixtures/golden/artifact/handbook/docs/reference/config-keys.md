# Configuration keys

| key | default | meaning |
|---|---|---|
| server.port | 8080 | plain HTTP port |
| server.tls_port | 8443 | HTTPS port when TLS is on |
| storage.path | /var/lib/service | local disk directory |
| log.level | info | log verbosity |
| cache.enabled | false | cache uploaded files in memory |

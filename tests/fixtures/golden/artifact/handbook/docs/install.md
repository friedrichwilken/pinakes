# Install

Install the service from the release archive.

## Download

Fetch the archive for your platform and verify its checksum.

## Unpack and run

```sh
tar xzf service-2.4.0-linux.tar.gz
./service serve --config service.toml
```

The server listens on port 8080 until TLS is configured.
